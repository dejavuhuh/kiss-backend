export kiss.web.authentication.SessionHistory

specification SessionHistorySpecification {
    id
    reason
    flat(user) {
        like/i(displayName)
    }
    ge(createdTime)
    le(createdTime)
}
