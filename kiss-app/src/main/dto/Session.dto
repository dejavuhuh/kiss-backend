export kiss.web.authentication.Session

specification SessionSpecification {
    id
    flat(user){
        like/i(displayName)
    }
    ge(createdTime)
    le(createdTime)
}

UserSession {
    id
    user {
        id
    }
    expiredTime
}