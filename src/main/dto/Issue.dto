export kiss.issue.Issue

input IssueInput {
    #allScalars(this)
}

specification IssueSpecification {
    like/i(title)
    like/i(description)
    eq(traceId)
    ge(createdTime)
    le(createdTime)
    associatedIdEq(creator)
}