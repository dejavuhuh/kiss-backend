export kiss.system.role.Role

specification RoleSpecification {
    like/i(name)

    ge(createdTime)

    le(createdTime)

    flat(permissions) {
        id as permissionId
    }
}

input RoleInput {
    #allScalars(this)
}
